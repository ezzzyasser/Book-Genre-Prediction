# Book-Genre-Prediction
This research project implements book genre prediction using deep learning. It evaluates three word embedding techniques—CBOW Word2Vec, Skip-gram, and GloVe—paired with CNN and LSTM architectures. The system processes a 4,658-record dataset, addressing class imbalance via sampling to accurately classify titles into 10 distinct genres.
